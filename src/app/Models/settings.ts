import {constants} from './constants';

export class Settings {
  fallingSpeed: number = constants.INIT_FALLING_SPEED;
  fallingFrequency: number = constants.INIT_FALLING_FREQUENCY;
  playerSpeed: number = constants.INIT_PLAYER_SPEED;
  gameTime: number = constants.INIT_GAME_TIME;
}
